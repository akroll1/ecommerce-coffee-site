extends layout

block content
	.container
		#admin-wrapper
			h1 Welcome, Admin.
				each account in accounts
					li #{account.username} - #{account.accessLevel}
					li #{account.city} - #{account.state}
